name: Build and publish docker image

on:
  push:
    branches: ['main']

jobs:
  build:
    permissions:
      contents: read
      packages: write
    uses: kneu-messenger-pigeon/github-workflows/.github/workflows/build.yaml@main
